#include <stdio.h>
#include <sys/stat.h>

int main() {
    struct stat st;
    stat("file.txt", &st);

    printf("Owner permissions: %d\n", st.st_mode & 0700);
    printf("Group permissions: %d\n", st.st_mode & 0070);
    printf("Others permissions: %d\n", st.st_mode & 0007);

    return 0;
}
