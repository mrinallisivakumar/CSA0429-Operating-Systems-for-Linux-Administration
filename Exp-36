#include <stdio.h>

int main() {
    int blocks[10], n, i;
    printf("Enter number of blocks: ");
    scanf("%d", &n);

    printf("Enter block numbers:\n");
    for (i = 0; i < n; i++)
        scanf("%d", &blocks[i]);

    printf("Linked allocation:\n");
    for (i = 0; i < n; i++)
        printf("%d -> ", blocks[i]);
    printf("NULL");

    return 0;
}
